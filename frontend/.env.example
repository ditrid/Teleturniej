# Skopiuj jako .env (tylko gdy potrzebujesz ręcznie wskazać backend — np. wdrożenie na Render).

# Adres backendu używany w produkcji. Zostaw puste (lub nie twórz pliku .env)
# przy pracy lokalnej — wtedy frontend używa ścieżek względnych i proxy Vite
# (vite.config.js) kieruje ruch na http://localhost:5000.
VITE_API_URL=https://teleturniej-vbd5.onrender.com
